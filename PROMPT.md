# 問題 md 作成プロンプト

AtCoder の問題ページを貼り付けて、このプロンプトごと AI に渡せば md ファイルが完成します。

---

## 使い方

1. AtCoder の問題ページを開いてテキストをコピー
2. 下の「AI へのお願い」と「テンプレート」と一緒に AI に貼り付ける
3. 出力された内容を `problems-raw/<id>.md` として保存
4. `cd src && go run . -f` でビルド

---

## AI へのお願い

下に競プロの問題文を貼ります。以下のテンプレートに従って md ファイルを作ってください。

### ルール

- **対象読者は小学生**（でも難しい問題も載せる）
- `# 問題の解説` は「この問題が何を求めているか」を小学生でもわかるように書く
  - **変数（N, K, L など）を使わない**。入力例の具体的な数字を使って説明する
  - 「切れ目が 3 か所あって、1 か所を選んで切る」のように書く
  - 入力の読み方も具体的な数字で説明する（`N` ← ではなく「3 ← 切れ目の数」）
- `# やさしい解説` は「どう解くか」のイメージを平易な言葉で（アルゴリズム名は書いてOK）
- `# 解説` は競プロ経験者向けの標準的な解説（計算量・正当性も書く）
- 各言語のコードは **各行にコメント** を入れて、何をしているか説明する
- コメントは日本語で書く
- コード内の変数名・関数名はそのまま（変えない）
- `difficulty` は以下の基準で設定する
  - 1: ABC A問題レベル（入出力、簡単な計算）
  - 2: ABC B問題レベル（ループ、条件分岐）
  - 3: ABC C問題レベル（典型アルゴリズム入門）
  - 4: ABC D問題レベル以上（二分探索・DP・グラフなど）
- `tags` は日本語で、アルゴリズム名・データ構造名・問題の特徴を書く
- `id` は `<コンテスト名>_<問題記号>` の形式（例: `abc300_a`, `typical90_a`）
- `file` は `id` の `_<問題記号>` を除いた部分（例: `abc300`, `typical90`）

---

## テンプレート

```
---
contest: （コンテスト名。例: ABC300, 典型90問）
atcoder_url: （問題のURL）
difficulty: （1〜4）
tags: [（タグ1）, （タグ2）, ...]
added_at: （今日の日付 YYYY-MM-DD）
id: （例: abc300_a）
file: （例: abc300）
---

# （問題番号） - （問題タイトル）（難易度マーク。例: ★4）

## 問題文

（原文をそのまま貼る。数式・変数は `バッククォート` で囲む）

## 制約

（原文をそのまま貼る）

## 入力

（原文をそのまま貼る）

## 出力

（原文をそのまま貼る）

## 入力例 1

\```text
（入力例）
\```

## 出力例 1

\```text
（出力例）
\```

### 解説

（小学生向けに、以下の流れで書く）

**この入力の意味：** ○○cm の〇〇に〇〇が○か所（具体的な数値）あって、○か所選んで〇つに分ける。

（全部の切り方・選び方を箇条書きで列挙。どれが最大かを ← これが最大！ で示す）

いちばん〇〇になる選び方は〇〇 → 答えは **○**。

（入力例 2, 3 ... も同様に続ける）

---

# 問題の解説

## この問題で何をするの？

（入力例1の具体的な数字を使って説明する。変数名（N, K, L など）は使わない。
 例：「34cm のようかんに切れ目が 3 か所あって、1 か所だけ選んで切る」のように書く）

## 入力の読み方

（具体的な数字で入力フォーマットを説明する。
 例：
   3 34   ← 「切れ目が 3 か所」「ようかんは 34cm」
   1      ← 「1 か所だけ選んで切る」
   8 13 26  ← 切れ目の場所
 のように書く）

## 出力の読み方

（何を出力するかを具体例で説明。「この例では 13 を出力する」のように）

---

# やさしい解説

## イメージ

（解法のイメージを日常の言葉で説明）

## どう解くか

（手順をステップで説明。難しい言葉は使わない）

---

# 解説

## 方針

（解法の方針。アルゴリズム名と使う理由）

（以下、必要なセクションを # で分けて書く）

---

# よくあるミス

## 1. （ミスの名前）

（ミスの内容と正しい書き方）

（必要なだけ続ける）

---

# 学びポイント

（この問題で学べること、競プロでの重要度）

---

# Python 解答

\```python
（各行にコメント付きの Python コード）
\```

---

# C++ 解答

\```cpp
（各行にコメント付きの C++ コード）
\```

---

# TypeScript 解答

\```typescript
（各行にコメント付きの TypeScript コード）
\```

---

# Ruby 解答

\```ruby
（各行にコメント付きの Ruby コード）
\```

---

# PHP 解答

\```php
（各行にコメント付きの PHP コード）
\```

---

# Rust 解答

\```rust
（各行にコメント付きの Rust コード）
\```

---

# Perl 解答

\```perl
（各行にコメント付きの Perl コード）
\```
```

---

## 問題文をここに貼り付けてください ↓

（ここに AtCoder の問題ページのテキストを貼る）
